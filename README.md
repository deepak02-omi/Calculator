# Simple Maven Calculator

This is a very small Java calculator application built with Maven.

## Run

```powershell
mvn compile
mvn exec:java
```

## Build JAR

```powershell
mvn package
java -jar target/calculator-app-1.0-SNAPSHOT.jar
```
